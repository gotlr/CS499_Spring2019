library(linearModels)
library(testthat)
context("linear_models")

test_that("linear models computes the same answer as R", {
  data(zip.train, package = "ElemStatLearn")
  #X.mat <-
  #y.vec <-
  #max.iterations <-
  #test all functions in LMSquareLossIterations
})
